name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Select Xcode Version
      run: sudo xcode-select -s /Applications/Xcode_11.4.app
    - name: Build
      run: |
        pod install --repo-update --clean-install
        xcodebuild test -workspace Giphy.xcworkspace -scheme thereto-ios -destination 'platform=iOS Simulator,name=iPhone 11 Pro,OS=13.7'
    - name: Run tests
      run: swift test -v
